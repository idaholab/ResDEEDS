# pnpm configuration for ResDEEDS web project

# Use isolated node linker for better dependency isolation
node-linker=isolated

# Enable pre/post scripts for compatibility
enable-pre-post-scripts=true

# Auto-install peer dependencies
auto-install-peers=true

# Hoist frequently used packages to reduce duplication
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*

# Disable shamefully-hoist for stricter dependency management
shamefully-hoist=false