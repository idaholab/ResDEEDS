export const environment = {
    production: true,
    apiUrl: '/api',
    drawioUrl: '/drawio',
    // URL for local Draw.io files served from Angular assets
    drawioLocalUrl: 'assets/drawio/main/webapp/index.html',
};