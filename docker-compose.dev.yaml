version: "3"

services:
  resilience:
    build:
      context: app
      dockerfile: Dockerfiles/Dockerfile.dev
    command: "tail -f /dev/null"
    ports:
      - 5000:5000
    volumes:
      - "./app:/opt/resilience_calculator"
